# . Which country has the highest number of confirmed cases? 
# SOLUTION
import pandas as pd

# Load your data into the DataFrame 'df' (replace 'your_data.csv' with your actual data file)
df = pd.read_csv('your_data.csv')  

country_with_least_confirmed = df.loc[df['Confirmed'].idxmin()]['Country/Region']
print(f"The country with the least number of confirmed cases is: {country_with_least_confirmed}")
